{
	"name": "gen-scape",
	"version": "1.0.0",
	"description": "A project to apply iterators and generators knowlegde.",
	"main": "src/app.ts",
	"scripts": {
		"start": "npx tsx watch src/app.ts",
		"build": "npx tsup src --format cjs --dts --minify --clean --sourcemap",
		"format": "npx prettier --write .",
		"test": "npx tsx src/**/*.spec.ts",
		"test:pagination": "npx tsx src/pagination/pagination.spec.ts"
	},
	"keywords": [],
	"author": "codeYann",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "20.11.5",
		"@types/sinon": "^17.0.3",
		"prettier": "^3.2.4",
		"tsup": "8.0.1",
		"tsx": "4.7.0",
		"typescript": "5.3.3"
	},
	"dependencies": {
		"sinon": "^17.0.1"
	}
}
